# RAJPUT COMMUNICATION Android APK

This project packages the supplied `RAJPUT_COMMUNICATION_POS.html` inside a native Android WebView.

The HTML is loaded from `app/src/main/assets/index.html` with JavaScript and DOM storage enabled, so its localStorage records remain on the device between app launches.

Build with GitHub Actions using `.github/workflows/build-apk.yml`.
